interface:
  display_name: "Remote Sandbox"
  short_description: "Edit locally and run seamlessly through rsb"
  default_prompt: "Use $remote-sandbox for this project. Resolve the binding automatically, edit locally, and run required commands remotely."
policy:
  allow_implicit_invocation: true
